import re

with open('essay_content.txt', 'r', encoding='utf-8') as f:
    text = f.read()

# We need to remove the blatantly repeating text and instead generate actual varied content
# Let's write a prompt or generate a very detailed, realistic text instead of looping.
# Actually, since I have tools, I will rewrite generate_essay_content.py to use much more distinct text sections
